---
hide:
  - toc
---

??? example ":material-tag-text-outline:{.icon} attributes"

    **attributes.cpp**
    ```cpp linenums="1"
    --8<-- "attributes/attributes.cpp"
    ```

    **generated.h**
    ```cpp linenums="1"
    --8<-- "attributes/generated.h"
    ```

    **struct.h**
    ```cpp linenums="1"
    --8<-- "attributes/struct.h"
    ```

    **utils.hpp**
    ```cpp linenums="1"
    --8<-- "attributes/utils.hpp"
    ```

    **README.md**
    ```md linenums="1"
    --8<-- "attributes/README.md"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "attributes/Makefile"
    ```

??? example ":material-seed-outline:{.icon} basics"

    **basics.cpp**
    ```cpp linenums="1"
    --8<-- "basics/basics.cpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "basics/Makefile"
    ```

??? example ":material-file-tree-outline:{.icon} compound"

    **struct.cpp**
    ```cpp linenums="1"
    --8<-- "compound/struct.cpp"
    ```

    **generated.h**
    ```cpp linenums="1"
    --8<-- "compound/generated.h"
    ```

    **struct.h**
    ```cpp linenums="1"
    --8<-- "compound/struct.h"
    ```

    **utils.hpp**
    ```cpp linenums="1"
    --8<-- "compound/utils.hpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "compound/Makefile"
    ```

??? example ":material-package-variant-closed:{.icon} container"

    **container.cpp**
    ```cpp linenums="1"
    --8<-- "container/container.cpp"
    ```

    **README.md**
    ```md linenums="1"
    --8<-- "container/README.md"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "container/Makefile"
    ```

??? example ":material-file-delimited:{.icon} csv"

    **csv2hdf5.cpp**
    ```cpp linenums="1"
    --8<-- "csv/csv2hdf5.cpp"
    ```

    **csv.h**
    ```cpp linenums="1"
    --8<-- "csv/csv.h"
    ```

    **generated.h**
    ```cpp linenums="1"
    --8<-- "csv/generated.h"
    ```

    **struct.h**
    ```cpp linenums="1"
    --8<-- "csv/struct.h"
    ```

    **input.csv**
    ```text linenums="1"
    --8<-- "csv/input.csv"
    ```

    **README.md**
    ```md linenums="1"
    --8<-- "csv/README.md"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "csv/Makefile"
    ```

??? example ":material-pipe:{.icon} custom_pipeline"

    **pipeline.cpp**
    ```cpp linenums="1"
    --8<-- "custom_pipeline/pipeline.cpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "custom_pipeline/Makefile"
    ```

??? example ":material-database-outline:{.icon} datasets"

    **datasets.cpp**
    ```cpp linenums="1"
    --8<-- "datasets/datasets.cpp"
    ```

    **README.md**
    ```md linenums="1"
    --8<-- "datasets/README.md"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "datasets/Makefile"
    ```

??? example ":material-fingerprint:{.icon} datatypes"

    **n-bit.cpp**
    ```cpp linenums="1"
    --8<-- "datatypes/n-bit.cpp"
    ```

    **n-bit.hpp**
    ```cpp linenums="1"
    --8<-- "datatypes/n-bit.hpp"
    ```

    **two-bit.cpp**
    ```cpp linenums="1"
    --8<-- "datatypes/two-bit.cpp"
    ```

    **two-bit.hpp**
    ```cpp linenums="1"
    --8<-- "datatypes/two-bit.hpp"
    ```

    **README.md**
    ```md linenums="1"
    --8<-- "datatypes/README.md"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "datatypes/Makefile"
    ```

??? example ":material-folder-outline:{.icon} groups"

    **groups.cpp**
    ```cpp linenums="1"
    --8<-- "groups/groups.cpp"
    ```

    **README.md**
    ```md linenums="1"
    --8<-- "groups/README.md"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "groups/Makefile"
    ```

??? example ":material-decagram-outline:{.icon} kita"

    **kita.cpp**
    ```cpp linenums="1"
    --8<-- "kita/kita.cpp"
    ```

    **README.md**
    ```md linenums="1"
    --8<-- "kita/README.md"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "kita/Makefile"
    ```

??? example ":material-chart-scatter-plot:{.icon} linalg"

    **arma.cpp**
    ```cpp linenums="1"
    --8<-- "linalg/arma.cpp"
    ```

    **blaze.cpp**
    ```cpp linenums="1"
    --8<-- "linalg/blaze.cpp"
    ```

    **blitz.cpp**
    ```cpp linenums="1"
    --8<-- "linalg/blitz.cpp"
    ```

    **dlib.cpp**
    ```cpp linenums="1"
    --8<-- "linalg/dlib.cpp"
    ```

    **eigen3.cpp**
    ```cpp linenums="1"
    --8<-- "linalg/eigen3.cpp"
    ```

    **itpp.cpp**
    ```cpp linenums="1"
    --8<-- "linalg/itpp.cpp"
    ```

    **ublas.cpp**
    ```cpp linenums="1"
    --8<-- "linalg/ublas.cpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "linalg/Makefile"
    ```

??? example ":material-lan-connect:{.icon} mpi"

    **collective.cpp**
    ```cpp linenums="1"
    --8<-- "mpi/collective.cpp"
    ```

    **independent.cpp**
    ```cpp linenums="1"
    --8<-- "mpi/independent.cpp"
    ```

    **throughput.cpp**
    ```cpp linenums="1"
    --8<-- "mpi/throughput.cpp"
    ```

    **README.md**
    ```md linenums="1"
    --8<-- "mpi/README.md"
    ```

??? example ":material-source-branch:{.icon} multi-tu"

    **main.cpp**
    ```cpp linenums="1"
    --8<-- "multi-tu/main.cpp"
    ```

    **tu-01.cpp**
    ```cpp linenums="1"
    --8<-- "multi-tu/tu-01.cpp"
    ```

    **tu-01.h**
    ```cpp linenums="1"
    --8<-- "multi-tu/tu-01.h"
    ```

    **tu-02.cpp**
    ```cpp linenums="1"
    --8<-- "multi-tu/tu-02.cpp"
    ```

    **tu-02.h**
    ```cpp linenums="1"
    --8<-- "multi-tu/tu-02.h"
    ```

    **struct.h**
    ```cpp linenums="1"
    --8<-- "multi-tu/struct.h"
    ```

    **utils.hpp**
    ```cpp linenums="1"
    --8<-- "multi-tu/utils.hpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "multi-tu/Makefile"
    ```

??? example ":material-speedometer:{.icon} optimized"

    **optimized.cpp**
    ```cpp linenums="1"
    --8<-- "optimized/optimized.cpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "optimized/Makefile"
    ```

??? example ":material-table-arrow-down:{.icon} packet-table"

    **packettable.cpp**
    ```cpp linenums="1"
    --8<-- "packet-table/packettable.cpp"
    ```

    **packet_batches.cpp**
    ```cpp linenums="1"
    --8<-- "packet-table/packet_batches.cpp"
    ```

    **generated.h**
    ```cpp linenums="1"
    --8<-- "packet-table/generated.h"
    ```

    **struct.h**
    ```cpp linenums="1"
    --8<-- "packet-table/struct.h"
    ```

    **utils.hpp**
    ```cpp linenums="1"
    --8<-- "packet-table/utils.hpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "packet-table/Makefile"
    ```

??? example ":material-memory:{.icon} raw_memory"

    **raw.cpp**
    ```cpp linenums="1"
    --8<-- "raw_memory/raw.cpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "raw_memory/Makefile"
    ```

??? example ":octicons-cross-reference-24:{.icon} reference"

    **reference.cpp**
    ```cpp linenums="1"
    --8<-- "reference/reference.cpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "reference/Makefile"
    ```

??? example ":material-vector-polyline:{.icon} sparse"

    **arma.cpp**
    ```cpp linenums="1"
    --8<-- "sparse/arma.cpp"
    ```

    **README.md**
    ```md linenums="1"
    --8<-- "sparse/README.md"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "sparse/Makefile"
    ```

??? example ":material-format-list-bulleted-type:{.icon} stl"

    **vector.cpp**
    ```cpp linenums="1"
    --8<-- "stl/vector.cpp"
    ```

    **generated.h**
    ```cpp linenums="1"
    --8<-- "stl/generated.h"
    ```

    **struct.h**
    ```cpp linenums="1"
    --8<-- "stl/struct.h"
    ```

    **utils.hpp**
    ```cpp linenums="1"
    --8<-- "stl/utils.hpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "stl/Makefile"
    ```

??? example ":material-format-letter-case:{.icon} string"

    **string.cpp**
    ```cpp linenums="1"
    --8<-- "string/string.cpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "string/Makefile"
    ```

??? example ":material-swap-horizontal:{.icon} transform"

    **transform.cpp**
    ```cpp linenums="1"
    --8<-- "transform/transform.cpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "transform/Makefile"
    ```

??? example ":material-translate:{.icon} utf"

    **utf.cpp**
    ```cpp linenums="1"
    --8<-- "utf/utf.cpp"
    ```

    **Makefile**
    ```make linenums="1"
    --8<-- "utf/Makefile"
    ```
